---
"@moonshot-ai/kimi-code": patch
---

Restrict the `/visual-model` picker to models that declare image, video, or audio input, so a text-only model can never be pinned as the visual companion. Run `/visual-model` to pick a media-capable model.
