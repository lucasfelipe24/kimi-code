---
"@moonshot-ai/kimi-code": minor
---

Add domain filtering to WebSearch with `allowed_domains` and `blocked_domains` options, and add a mandatory Sources section to web search results. FetchURL now includes an optional prompt parameter for targeted content extraction, automatic caching, a preapproved host list for common documentation sites, manual redirect control, and binary content detection.
