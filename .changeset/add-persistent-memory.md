---
"@moonshot-ai/kimi-code": minor
---

Add persistent memory as a native engine-v2 capability: the `Memory` tool, scoped selective recall, and explicit writes are on by default, with no experiment to enable. Automatic extraction stays opt-in through `KIMI_CODE_EXPERIMENTAL_PERSISTENT_MEMORY_AUTO_EXTRACT`.
