---
"@moonshot-ai/kimi-code": minor
---

Add experimental persistent memory to the v2 engine with scoped recall and explicit writes. Enable `KIMI_CODE_EXPERIMENTAL_PERSISTENT_MEMORY`; automatic extraction requires the separate `KIMI_CODE_EXPERIMENTAL_PERSISTENT_MEMORY_AUTO_EXTRACT` opt-in.
