---
"@moonshot-ai/kimi-code": minor
---

Make dynamic workflows reachable and proactive: the main agent now has the Workflow tool enabled and automatically enters workflow mode for large or multi-phase tasks, proposing a workflow without needing /workflow on first. In yolo/auto permission modes, workflow runs are approved automatically instead of always asking.
