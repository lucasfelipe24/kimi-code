---
"@moonshot-ai/kimi-code": minor
---

Add Brave Search as a selectable web search provider (enabled by default) with a suite of specialized search tools. Configure it under Settings → Web Search or with `kimi search set brave --api-key ...` and select it with `kimi search use brave`.
