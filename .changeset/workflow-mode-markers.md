---
"@moonshot-ai/kimi-code": patch
---

Render Dynamic Workflow mode toggles as transcript markers in the Swarm style instead of plain status lines, including when the agent enables the mode. Run /workflow on or /workflow off to see them.

Fix the Workflow tool rejecting calls that pass an empty `name` alongside a `script`.
