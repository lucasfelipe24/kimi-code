---
"@moonshot-ai/kimi-code": minor
---

Add the Monitor built-in tool to watch a background command and turn each stdout line into an event notification that wakes the agent. Ask the agent to monitor a log, poll, or watcher; use a persistent monitor for streaming and stop it with TaskStop.
