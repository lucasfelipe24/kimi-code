Metadata-Version: 2.4
Name: agent-gw
Version: 0.2.5
Summary: Kimi agent-gw（Kimi For Coding Backend）的 Python SDK —— 覆盖 tools/chat/messages/embeddings/search/fetch 等接口。
Author: Moonshot AI
License: Proprietary
Project-URL: Homepage, https://github.com/MoonshotAI
Keywords: kimi,moonshot,agent-gw,codegw,llm
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3 :: Only
Classifier: Intended Audience :: Developers
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Requires-Dist: requests>=2.28.0
Provides-Extra: dev
Requires-Dist: pytest>=7; extra == "dev"
Requires-Dist: responses>=0.23; extra == "dev"

# agent-gw Python SDK

**Kimi agent-gw**（Kimi For Agent Backend）的同步 Python 客户端 ——
该网关聚合了 chat completions、NLP 工具、Web 搜索，以及
`/v1/tools` 工具分发器（用于 `get_stock_realtime_price`、`nlp_tokenize`、
`call_data_source_tool` 等方法）。

## 安装

> 公司内部使用。当前发版方式以 git tag 为准（`v<version>`），
> 版本号同 `agent_gw/_version.py`。仓库地址：
> `git@dev.msh.team:leixun/agent-gw-pysdk.git`。

要求 Python ≥ 3.8、`requests` ≥ 2.28。

### 方式 ①：内网安装（推荐，走公司 Nexus PyPI）

```bash
# 最新版
pip3 install -i https://pypi.msh.team/simple agent-gw

# 指定版本
pip3 install -i https://pypi.msh.team/simple agent-gw==0.2.0
```

`requirements.txt` 写法：把 `--index-url` 写进文件首行（或者全局配在
`~/.pip/pip.conf` 的 `index-url` 里），其它项目正常 `agent-gw==0.2.0`。

### 方式 ②：外网安装（走 CDN 直链）

```bash
# 指定版本
pip3 install https://cdn.kimi.com/agentgw/pysdk/v0.2.0/agent_gw-0.2.0-py3-none-any.whl

# 最新版（用 manifest.json 探出最新 URL）
pip3 install "$(curl -s https://cdn.kimi.com/agentgw/pysdk/manifest.json \
    | python3 -c "import json,sys; print(json.load(sys.stdin)['latest']['url'])")"
```

`requirements.txt` 写法（指定版本）：

```
agent-gw @ https://cdn.kimi.com/agentgw/pysdk/v0.2.0/agent_gw-0.2.0-py3-none-any.whl
```

## 快速开始

```python
from agent_gw import AgentGwClient

client = AgentGwClient(
    api_key="sk-...",
    base_url="https://agent-gw-dev.dev.kimi.team/coding",  # ⚠️ 末尾 /coding 必填，是 agent-gw 的服务挂载路径
    skill="stock-assistant",                              # 设置 X-Kimi-Skill 请求头
)
# 兼容写法：如果你的老配置里 base_url 带 ".../v1/tools" 或 ".../v1" 后缀，
# SDK 会自动剥掉这两个后缀（_strip_tool_suffix），保留 /coding 部分。

# /v1/tools 工具分发器
rsp = client.tools.stock_realtime_price(
    ticker="600519.SH",
    time="2025-06-08 10:30:00",
    type="realtime_price",
)
if rsp.is_success:
    data = rsp.json()      # 把 result.user[0].text 解析成 JSON
elif rsp.is_rate_limited:
    ...                    # 触发限速，建议退避
else:
    print(rsp.error)
```

### 配置的三档兜底

`api_key` / `base_url` / `kimi_chat_id` 都按下面这套顺序解析（**取第一个非空**）：

| 优先级 | 来源                                                                  | api_key         | base_url           | kimi_chat_id             |
| ------ | --------------------------------------------------------------------- | --------------- | ------------------ | ------------------------ |
| ①      | `AgentGwClient(api_key=..., base_url=..., kimi_chat_id=...)` 显式传入 | ✓               | ✓                  | ✓                        |
| ②      | 环境变量                                                              | `KIMI_API_KEY`  | `KIMI_BASE_URL`    | `KIMI_CHAT_ID`           |
| ③      | JSON 文件 `~/.kimi/agent-gw.json`                                     | `api_key` 字段  | `base_url` 字段    | `kimi_chat_id` 字段      |
| 兜底   | （没拿到的行为）                                                      | 抛 `ValueError` | `DEFAULT_BASE_URL` | 不发 `X-Kimi-Chat-Id` 头 |

JSON 文件示例：

```json
{
  "api_key":      "sk-...",
  "base_url":     "https://agent-gw-dev.dev.kimi.team/coding",
  "kimi_chat_id": "<可选；存在则所有请求都会带 X-Kimi-Chat-Id 头>"
}
```

落盘命令：

```bash
mkdir -p ~/.kimi && chmod 700 ~/.kimi
cat > ~/.kimi/agent-gw.json <<'JSON'
{
  "api_key":  "sk-...",
  "base_url": "https://agent-gw-dev.dev.kimi.team/coding"
}
JSON
chmod 600 ~/.kimi/agent-gw.json
```

`kimi_chat_id` 是可选字段：解析到非空值（不论来自参数、env 还是 JSON）时，
SDK 会给所有发往 agent-gw 的请求带上 HTTP 头 `X-Kimi-Chat-Id`，取值就是这个字符串；
都没配则该头不出现。

所以最简用法：

```python
AgentGwClient()                                    # 全从 env / JSON 读
AgentGwClient(api_key="sk-...")                    # 只覆盖 api_key
AgentGwClient(base_url="https://other/coding")     # 只覆盖 base_url
```

也可以 `from agent_gw import API_KEY_ENV_VAR, BASE_URL_ENV_VAR, CONFIG_FILE`
拿到常量名做自检。配置文件 JSON 解析失败会抛 `ValueError`（不会被静默吃掉，
保证配置错误立刻可见）。

### User-Agent

**默认值**：`Kimi AgentGW PySDK/<version>`（同 `agent_gw.DEFAULT_USER_AGENT`，版本号取自 `__version__`）。

**覆盖方式**：构造时传 `user_agent=`：

```python
from agent_gw import AgentGwClient, DEFAULT_USER_AGENT

print(DEFAULT_USER_AGENT)   # → 'Kimi AgentGW PySDK/0.1.0'

# 自定义 UA（推荐写成 `<产品名>/<版本> (<额外信息>)` 的格式）
client = AgentGwClient(user_agent="MyAgent/1.2.3 (Windows)")

# 不传 = 用默认 DEFAULT_USER_AGENT
client = AgentGwClient()
```


## 接口覆盖

| 路由                             | 方法                | 说明                                  |
| -------------------------------- | ------------------- | ------------------------------------- |
| `GET  /v1/models`                | `list_models()`     | 列出可用模型                          |
| `POST /v1/chat/completions`      | `chat_completion()` | `stream=True` 返回 SSE 迭代器         |
| `POST /v1/messages`              | `messages()`        | `stream=True` 返回 SSE 迭代器         |
| `POST /v1/messages/count_tokens` | `count_tokens()`    | 仅计算 token 数                       |
| `POST /v1/embeddings`            | `embeddings()`      | 文本向量化                            |
| `POST /v1/search`                | `search(query)`     | Web 搜索                              |
| `POST /v1/fetch`                 | `fetch(url)`        | `as_markdown=True` 返回 text/markdown |
| `POST /v1/files`                 | `upload_file()`     | multipart 文件上传（透传到 OpenGW）   |
| `POST /v1/tools`                 | `tools.invoke()`    | 工具分发器，另有类型化 helper（见下） |

### Tools 分发器 helper

```python
client.tools.invoke("call_data_source_tool", {...})
client.tools.stock_realtime_price(ticker=..., time=..., type=...)
client.tools.nlp_tokenize("文本")
client.tools.nlp_normalize("文本")
client.tools.nlp_shortkeys("文本")
client.tools.nlp_embedding(["a", "b"])
client.tools.call_data_source_tool({...})
client.tools.get_data_source_desc({...})
```

上面所有 `tools.*` helper 都返回 `ToolResponse` —— 对 agent-gw `/v1/tools` 信封的薄包装：

| 属性                 | 含义                                          |
| -------------------- | --------------------------------------------- |
| `raw`                | 完整 JSON 信封（dict）                        |
| `is_success`         | 顶层 `is_success` 字段                        |
| `error`              | 顶层 `error` 字段（字符串）                   |
| `is_rate_limited`    | body 中包含 429/rate/limit 时为 `True`        |
| `text`               | `result.user[0].text`（内层 JSON 字符串原文） |
| `json()`             | 把 `text` 解析为 JSON                         |
| `raise_for_status()` | `is_success=False` 时抛出 `ToolError`         |

## 异常体系

```python
from agent_gw import (
    AgentGwError,        # 所有异常的基类
      TransportError,      # 网络/超时（请求未送达）
      APIError,            # 4xx / 5xx（携带 .status_code、.body、.request_id）
        AuthenticationError,   # 401，API key 无效/过期
        PaymentRequiredError,  # 402，需要付费/会员
        QuotaExceededError,    # 403，额度耗尽
        NotFoundError,         # 404，路由或方法不存在
        RateLimitError,        # 429，限速（携带 .retry_after）
        ServerError,           # 5xx，服务端或下游故障
      ToolError,           # /v1/tools 信封 is_success=false
)
```

## 流式（SSE）

`chat_completion(..., stream=True)` 与 `messages(..., stream=True)` 返回生成器，
逐条 yield 解析后的 SSE `data:` 内容（dict）。遇到 `[DONE]` 自动结束。

## 如何运行测试

```bash
cd /Users/moonshot/codes/kimi-agent-workspace/agent-gw-pysdk

# 1) 安装 dev 依赖（pytest）
pip install -e '.[dev]'
# 或者：python3 -m pip install --user pytest

# 2) 跑全部测试
python3 -m pytest tests/ -q
# 详细输出：
python3 -m pytest tests/ -v
# 跑单个文件 / 单个用例：
python3 -m pytest tests/test_tools.py -v
python3 -m pytest tests/test_endpoints.py::test_chat_completion_streaming_iterates_data_chunks -v
```

测试覆盖（75 条用例，全部 mock HTTP，无须真实 API key）：

| 文件                      | 覆盖范围                                                  |
| ------------------------- | --------------------------------------------------------- |
| `tests/conftest.py`       | 共享 `FakeSession` / `FakeResponse` 夹具                  |
| `tests/test_client.py`    | 构造参数、base_url 处理、请求头、生命周期                 |
| `tests/test_errors.py`    | 401/402/403/404/429/5xx 异常映射、Retry-After、网络错误   |
| `tests/test_tools.py`     | `ToolResponse` 全部属性 + `ToolsAPI` 所有 helper          |
| `tests/test_endpoints.py` | 9 个 HTTP 接口（含 SSE 流式 / Markdown fetch / 204 响应） |

## 如何运行示例

```bash
cd /Users/moonshot/codes/kimi-agent-workspace/agent-gw-pysdk
export KIMI_API_KEY=sk-...          # 必填
export KIMI_BASE_URL=https://agent-gw-dev.dev.kimi.team/coding   # 可选，默认就是这个
export KIMI_MODEL=kimi-k2-0905-preview            # 可选

python3 examples/list_models.py
python3 examples/chat_completion.py
python3 examples/messages.py
python3 examples/count_tokens.py
python3 examples/embeddings.py
python3 examples/search_and_fetch.py
python3 examples/file_upload.py
python3 examples/stock_price.py
python3 examples/tools_general.py
python3 examples/error_handling.py
```

每个示例的具体作用见 [`examples/README.md`](examples/README.md)。

## 关于 stock-assistant 的迁移示例

`skr/skills/stock-assistant/scripts/data_fetch.py` 与 `monitor.py`
已经从直接 `requests.post(...)` 切到本 SDK：

- 通过 `_kimi_client(kimi_cfg)` 缓存复用 `AgentGwClient`；
- 三处旧的 `get_stock_realtime_price` POST 调用全部替换为 `client.tools.stock_realtime_price(...)`；
- 限速 / 网络错误改用 SDK 提供的 `RateLimitError` / `TransportError` / `APIError`；
- 原有 `_load_kimi_cfg()` 与 `kimiPluginBaseUrl` 配置完全兼容（SDK 自动剥离 `/v1/tools` 后缀）。

可作为其他调用方迁移的参考。

## 发布新版本（给维护者）

**版本号唯一真值 = git tag**（形如 `v0.2.0`）。仓库用
[`setuptools_scm`](https://setuptools-scm.readthedocs.io/) 在构建/安装时
从 `git describe` 自动推导版本号，写出 `agent_gw/_version.py`（已 `.gitignore`，
**不要手动改**）。`pyproject.toml` 也不再保存版本号，`version` 字段是 `dynamic`。

版本号遵守 [semver](https://semver.org/lang/zh-CN/)：

| 改动类型               | 版本号 bump 规则           |
| ---------------------- | -------------------------- |
| 仅 bug fix             | `0.1.0` → `0.1.1`（PATCH） |
| 新增接口、向后兼容     | `0.1.0` → `0.2.0`（MINOR） |
| 不兼容改动（破坏 API） | `0.1.0` → `1.0.0`（MAJOR） |

### 各种状态下的版本字符串

| 仓库状态                      | 解析出的版本                        |
| ----------------------------- | ----------------------------------- |
| 干净的 tag commit（`v0.2.0`） | `0.2.0`                             |
| tag 之后还有 3 个 commit      | `0.2.1.dev3+g<sha>`                 |
| 工作树有未提交改动            | `0.2.1.dev3+g<sha>.d<date>`         |
| 完全没有任何 tag（极端情况）  | `0.0.0+local`（`fallback_version`） |

dev 安装（`pip install -e .` / 纯 clone）默认就是 dev 版本号，能一眼看出
"我用的不是正式 release"。

### 发版流程

发版完全由 **GitLab CI** 自动化（见 `.gitlab-ci.yml`）。维护者只需要做两件事：
跑测试 → 打 tag。剩下的 build / 上传都是 CI 干。

```bash
# 1. 跑测试 + 烟测
python3 -m pytest tests/ -q
python3 examples/list_models.py

# 2. 打 tag + push（推 tag 触发流水线，使用方按 tag 装）
git tag v0.2.0                              # 在要发的 commit 上打
git push origin master
git push origin v0.2.0
```

> 没有任何"改文件、commit、再打 tag"的步骤 —— 直接 `git tag v0.2.0` 就行。
> 想确认版本号能正确推出来：``python3 -c "from setuptools_scm import get_version; print(get_version())"``。

### CI 在做什么

推 `vX.Y.Z` tag 后，`.gitlab-ci.yml` 顺序跑：

1. **test** — `pytest tests/ -q` 兜底。
2. **build:wheel** — `setuptools_scm` 从 tag 推版本号，`python -m build --wheel` 产出
   `dist/agent_gw-X.Y.Z-py3-none-any.whl`。
3. **publish:nexus** — `twine upload` 推到内网 Nexus PyPI（`$NEXUS_PYPI_URL`）。
4. **publish:tos** — 把新 wheel 归档到 `cdn/vX.Y.Z/`，重跑 `scripts/regen_index.sh`
   刷新 `index.html` / `manifest.json`，最后用
   `scripts/upload_cdn_to_tos.py`（火山引擎 TOS Python SDK）把整个 `cdn/` 目录同步到
   `tos://prod-chat-kimi/agentgw/pysdk/`。

### CI 需要的 Variables

在 GitLab → Settings → CI/CD → Variables 里维护：

| 变量名                | 用途                             | 建议属性          |
| --------------------- | -------------------------------- | ----------------- |
| `NEXUS_PYPI_URL`      | 内网 Nexus PyPI upload URL       | Protected         |
| `NEXUS_PYPI_USERNAME` | Nexus 上传账号                   | Protected         |
| `NEXUS_PYPI_PASSWORD` | Nexus 上传密码                   | Protected, Masked |
| `TOS_ACCESS_KEY`      | 火山引擎 AK                      | Protected, Masked |
| `TOS_SECRET_KEY`      | 火山引擎 SK                      | Protected, Masked |
| `TOS_ENDPOINT`        | 例如 `tos-cn-beijing.volces.com` | Protected         |
| `TOS_REGION`          | 例如 `cn-beijing`                | Protected         |

`TOS_BUCKET`（`prod-chat-kimi`）和 `TOS_PREFIX`（`agentgw/pysdk/`）已经写死在
`.gitlab-ci.yml` 的 `publish:tos` job 里，按需覆盖即可。

> Variables 建议都打 **Protected** —— 只有 protected tag（如 `v*`）才能读到密钥，
> 防止从普通分支偷跑。

### 通知使用方

push tag 之后通知调用方 bump 版本号（改一处 `requirements.txt` /
`pyproject.toml` 即可）。Breaking change 的 release 一定要在 commit / tag
message 里写清楚迁移说明。

## 协议

Proprietary —— Moonshot AI 内部使用。
