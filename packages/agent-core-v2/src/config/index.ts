/**
 * `config` domain barrel — re-exports the config contract (`config`) and its
 * scoped services (`configService`). Importing this barrel registers the config
 * bindings into the scope registry.
 */

export * from './config';
export * from './configService';
export * from './kimi-env-params';
export * from './path';
export * from './schema';
export * from './thinking';
