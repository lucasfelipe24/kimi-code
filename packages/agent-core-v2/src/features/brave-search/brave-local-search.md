Search Brave local locations and resolve matching opaque IDs into POI details, with optional descriptions.

Location IDs are ephemeral (typically usable for about eight hours), are limited to 20 per call, and are never persisted. Optional locale and metric/imperial unit controls apply only to local detail requests.