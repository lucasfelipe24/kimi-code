Check a query with Brave Spellcheck, optionally scoped to a country.

An empty response means no suggestion was returned, which can indicate that the query is already correct or that no correction is available.