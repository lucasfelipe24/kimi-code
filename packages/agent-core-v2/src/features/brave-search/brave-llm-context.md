Retrieve Brave LLM Context grounding for a short query using GET or POST.

Supports bounded source, token, and snippet controls plus optional validated location headers. The output preserves generic, map, and POI grounding and source metadata as structured JSON; nested JSON snippets are not flattened.